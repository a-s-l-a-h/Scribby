<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ScribbyApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:ScribbyApp.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="ScribbyApp">

    <TabBar>

        <!-- Tab 1: Home Page -->
        <Tab Title="Home" Icon="home.png">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate views:HomePage}"
                Route="HomePage" />
        </Tab>

        <!-- Tab 2: Connection Page -->
        <Tab Title="Connect" Icon="bluetooth.png">
            <ShellContent
                Title="Connect"
                ContentTemplate="{DataTemplate views:ConnectPage}"
                Route="ConnectPage" />
        </Tab>

        <!-- Tab 3: Control Page -->
        <Tab Title="Control" Icon="settings.png">
            <ShellContent
                Title="Control"
                ContentTemplate="{DataTemplate views:ControlPage}"
                Route="ControlPage" />
        </Tab>

        <!-- Tab 4: Scripting Page -->
        <Tab Title="Script" Icon="code.png">
            <ShellContent
                Title="Script"
                ContentTemplate="{DataTemplate views:ScriptPage}"
                Route="ScriptPage" />
        </Tab>

    </TabBar>

</Shell>